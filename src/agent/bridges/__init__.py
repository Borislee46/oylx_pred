"""Bridge layer between agent and pages.

Core logic extracted from src/pages/prediction/form_bridge.py.
Pages layer re-exports these so existing imports continue to work.

Part of AI Harness Phase 0: dependency direction fix (agent ← pages → agent).
"""
